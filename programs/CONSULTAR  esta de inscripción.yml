info:
  name: CONSULTAR  esta de inscripción
  type: http
  seq: 5

http:
  method: GET
  url: "{{URLBaseApi}}/api/programs/:slug/enroll"
  params:
    - name: slug
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
