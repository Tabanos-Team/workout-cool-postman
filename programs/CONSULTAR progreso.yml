info:
  name: CONSULTAR progreso
  type: http
  seq: 6

http:
  method: GET
  url: "{{URLBaseApi}}/api/programs/:slug/enroll"
  params:
    - name: slug
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
