info:
  name: Detalle de sesion de programa
  type: http
  seq: 3

http:
  method: GET
  url: "{{URLBaseApi}}/api/programs/:slug/sessions/:sessionSlug"
  params:
    - name: sessionSlug
      value: ""
      type: path
    - name: slug
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
