info:
  name: Inscribir usuario en programa
  type: http
  seq: 4

http:
  method: POST
  url: "{{URLBaseApi}}/api/programs/:slug/enroll"
  params:
    - name: slug
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
