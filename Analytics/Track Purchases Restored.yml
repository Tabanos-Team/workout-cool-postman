info:
  name: Track Purchases Restored
  type: http
  seq: 5
  tags:
    - analytics
    - premium

http:
  method: POST
  url: "{{URLBaseApi}}/analytics/premium"
  body:
    type: json
    data: |-
      {
        "event": "paywall_restored"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const body = typeof res.body === "string" ? JSON.parse(res.body || "{}") : res.body;

        test("tracks purchases restored event", function() {
          expect(res.status).to.equal(200);
          expect(body).to.include({
            event: "paywall_restored",
            success: true
          });
        });

        test("maps restore to the expected analytics event", function() {
          expect(body.analyticsEvent).to.include({
            name: "Purchases Restored",
            channel: "premium"
          });
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Covers POST /api/analytics/premium for the paywall_restored event.
