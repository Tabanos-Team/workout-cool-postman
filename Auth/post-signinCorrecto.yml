info:
  name: post-signinCorrecto
  type: http
  seq: 1

http:
  method: POST
  url: "{{URLBaseApi}}/auth/sign-in/email"
  body:
    type: json
    data: |-
      {
        "email": "verdoso64@gmail.com",
        "password": "456264123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
