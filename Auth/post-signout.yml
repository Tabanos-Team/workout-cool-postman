info:
  name: post-signout
  type: http
  seq: 2

http:
  method: POST
  url: "{{URLBaseApi}}/auth/sign-out"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
