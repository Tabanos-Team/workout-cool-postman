info:
  name: Sign-in
  type: http
  seq: 1

http:
  method: POST
  url: "{{URLBaseApi}}/auth/sign-in/email"
  body:
    type: json
    data: |-
      {
        "email": "test@gmail.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
