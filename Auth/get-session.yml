info:
  name: get-session
  type: http
  seq: 7

http:
  method: GET
  url: "{{URLBaseApi}}/auth/session"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
