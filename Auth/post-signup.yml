info:
  name: post-signup
  type: http
  seq: 3

http:
  method: POST
  url: "{{URLBaseApi}}/auth/signup"
  body:
    type: json
    data: |-
      {
        "email": "verdoso64@gmail.com",
        "password": "456264123",
        "firstName": "Christian",
        "lastName": "Zapana"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
