info:
  name: post-signinIncorrecto
  type: http
  seq: 6

http:
  method: POST
  url: "{{URLBaseApi}}/auth/sign-in/email"
  body:
    type: json
    data: |-
      {
        "email": "verdoso64@gmail.com",
        "password": "123456789"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
